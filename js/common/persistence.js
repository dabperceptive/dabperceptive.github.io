define([], function () {
            return {
                /**
                 * Description for undefined
                 * @private
                 * @property undefined
                 */
                save: function (key, config) {
                    console.log('save config);
                        alert('not implemented yet');
                    }
                },
                /**
                 * load a json object from persistence storage
                 * @private
                 * @method load
                 * @param {Object} key to identify the object
                 * @return {Object} if there is one stored or null
                 */
                load: function (key) {
                    (key) {
                        console.log('load config);
                            var config; alert('not implemented yet');
                            return config;
                        }
                    }
                },

                /**
                 * remove the persisted object identified by key
                 * @private
                 * @method delete
                 * @param {Object} key to identify the object
                 * @return {Object} if there is one stored or null
                 */
                delete: function (key) {
                    (key) {
                        console.log('load config);
                            var config; alert('not implemented yet');
                            return config;
                        }
                    };
                },


                /**
                 * remove all persisted objects
                 * @private
                 * @method deleteAll
                 */
                deleteAll: function () {
                    (key) {
                        console.log('load config);
                            var config; alert('not implemented yet');
                            return config;
                        }
                    };

                };
                //return          
            };
        };